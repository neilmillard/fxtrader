{% extends 'layout.twig' %}

{% block page_title %}{{ parent() }} | Rotas{% endblock %}

{% block content %}
    <div class="page-header">
        <h3>Admin Home</h3>
</div>
    <div>
        <a class="btn" href="{{ base_url() }}{{ path_for('users') }}">Manage Users</a>
    </div>
<div>
    <a class="btn" href="{{ base_url() }}{{ path_for('adminstrategies') }}">Manage Strategies</a>
</div>
    <div>
        <a class="btn" href="{{ base_url() }}{{ path_for('adminqueues') }}">View JobQueues Index</a>
    </div>
    <div>
        <a class="btn" href="{{ base_url() }}{{ path_for('fetchcandlesday') }}">Trigger fetch day candles</a>
    </div>
<br/><br/>

{% endblock %}